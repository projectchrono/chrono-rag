"""Thin surfaces over the retrieval core: MCP server, CLI, and the web answer
path. Each is a separate adapter; none reaches around the core.

This package is intentionally NOT named `mcp` so it does not shadow the installed
`mcp` library that the MCP server imports.
"""
